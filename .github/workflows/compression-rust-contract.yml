name: Compression Rust Contract

on:
  workflow_dispatch:
  pull_request:
    paths:
      - ".github/workflows/compression-rust-contract.yml"
      - "apps/windows/**"
      - "docs/rust-migration-report.md"
      - "docs/rust-migration-status.json"
      - "package.json"
      - "package-lock.json"
      - "src/compactService.js"
      - "src/compressionContract.js"
      - "src/contextBudget.js"
      - "src/eventStore.js"
      - "test/fixtures/compression-json-sidecar.js"
      - "test/compressionBenchmark.test.js"
      - "test/rustCompressionContract.test.js"
      - "tools/compression/**"
      - "tools/check-rust-migration-status.mjs"
  push:
    branches:
      - main
      - "codex/**"
    paths:
      - ".github/workflows/compression-rust-contract.yml"
      - "apps/windows/**"
      - "docs/rust-migration-report.md"
      - "docs/rust-migration-status.json"
      - "package.json"
      - "package-lock.json"
      - "src/compactService.js"
      - "src/compressionContract.js"
      - "src/contextBudget.js"
      - "src/eventStore.js"
      - "test/fixtures/compression-json-sidecar.js"
      - "test/compressionBenchmark.test.js"
      - "test/rustCompressionContract.test.js"
      - "tools/compression/**"
      - "tools/check-rust-migration-status.mjs"

permissions:
  contents: read

concurrency:
  group: compression-rust-contract-${{ github.ref }}
  cancel-in-progress: true

jobs:
  compression-rust-contract:
    name: Fixture and Rust parity
    runs-on: windows-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Node
        uses: actions/setup-node@v6
        with:
          node-version: "22"
          cache: npm

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install dependencies
        run: npm ci

      - name: Build release sidecar
        run: cargo build --release --manifest-path apps/windows/Cargo.toml

      - name: Check migration manifest
        run: npm run rust:migration:check

      - name: Run compression contract
        run: npm run compression:contract

      - name: Run compression Rust unit tests
        run: cargo test --manifest-path apps/windows/Cargo.toml compression_sidecar

      - name: Measure current Node compression hot path
        run: npm run compression:benchmark -- --synthetic-only --output .tmp/compression-node-benchmark-ci.json

      - name: Upload benchmark output
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: compression-node-benchmark
          path: .tmp/compression-node-benchmark-ci.json
          if-no-files-found: ignore
